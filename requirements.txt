# VerbaFlow - Dependências
# Python 3.12 é obrigatório para compatibilidade com CrewAI/Pydantic

# Core Framework
crewai>=0.1.0
crewai-tools>=0.1.0

# LLM Integration
langchain-groq>=0.1.0
langchain>=0.1.0

# Web Search
tavily-python>=0.3.0

# Data Processing
pandas>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Web Interface
streamlit>=1.28.0

# Jupyter Notebook
jupyter>=1.0.0
ipykernel>=6.25.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Gemini API (Fallback)
# NOTA: Há conflito de versões entre google-generativeai e langchain-google-genai
# O provider nativo do CrewAI (crewai[google-genai]) é recomendado para evitar conflitos
langchain-google-genai>=3.1.0  # Versão mais recente (pode ter conflito com google-generativeai antiga)
google-generativeai>=0.8.0  # Versão mínima (pode conflitar com langchain-google-genai>=3.1.0)
litellm>=1.0.0  # Necessário para CrewAI suportar Gemini
# Provider nativo do Gemini para CrewAI (opcional, mas recomendado para fallback)
# Instale com: pip install 'crewai[google-genai]' ou descomente a linha abaixo:
# crewai[google-genai]>=0.1.0

